import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['images.unsplash.com', 'play-lh.googleusercontent.com', 'simracingsetup.com', 'images.financialexpressdigital.com'],
  },
};

export default nextConfig;
